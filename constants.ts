export const ARTWORK_GENERATION_DAILY_LIMIT = 3; // Total generations allowed per day per user
export const ARTWORK_GENERATION_MIN_INTERVAL_MS = 10000; // 10 seconds between generations
export const ARTWORK_GENERATION_GLOBAL_DAILY_LIMIT = 15; // Total generations allowed per day

export const TOKEN_NAME = "Ludum";
